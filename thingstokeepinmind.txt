sudo nano /etc/ssh/sshd_config
sudo systemctl restart sshd

*virtual env*
cd book_blog
source venv/bin/activate

**nginx**

sudo nano /etc/nginx/sites-enabled/bookblog
sudo nano /etc/nginx/nginx.conf
sudo systemctl restart nginx

**gunicorn**

gunicorn -w 3 run:app

MAC VENV: . .venv/bin/activate

npx tailwindcss -i ./bookblog/static/stylesheet.css -o ./bookblog/static/output.css --watch

supervisorctl reread
supervisorctl update
service supervisor restart
supervisorctl restart

pip3 install spacytextblob
pip install textblob
python -m textblob.download_corpora
python3 -m spacy download en_core_web_sm
python-dotenv

####### POSTGRES CONFIG ##########

robert@Roberts-Air ~ % brew services start postgresql

initdb /usr/local/var/postgres
psql postgres
postgres=# CREATE ROLE robert WITH LOGIN PASSWORD 'Waterball$5';
postgres=# ALTER ROLE robert CREATEDB;
ALTER ROLE
postgres=# sudo -iu postgres psql
postgres-# CREATE DATABASE trip_planner;
postgres=# CREATE DATABASE trip_planner;
CREATE DATABASE
postgres=# GRANT ALL PRIVILEGES ON DATABASE trip_planner TO robert;
GRANT
postgres=# \l
postgres=# \q
(.venv) robert@Roberts-Air postgres_api_trip_reminder % python init_postgres.py

https://www.cherryservers.com/blog/how-to-install-and-setup-postgresql-server-on-ubuntu-20-04